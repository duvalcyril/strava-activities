{% autoescape false %}
{% for activity in activities %}{% for imagePath in activity.getLocalImagePaths() %}<img src="{{ imagePath }}" alt="{{ activity.getName()|replace({'"':'\''}) }}" title="{{ activity.getName()|replace({'"':'\''}) }}" width="75" />{% endfor %}{% endfor %}
{% endautoescape %}