Since `{{ totals.getStartDate().format('d-m-Y') }}` I recorded a total distance of `{{ totals.getDistance()|number_format(0, '.', ' ') }} km`,
an elevation of `{{ totals.getElevation()|number_format(0, '.', ' ') }} m`
and a total time of `{{ totals.getMovingTimeFormatted() }}` 🎉.